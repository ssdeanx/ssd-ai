runtime: "container"

build:
  dockerfile: "Dockerfile"
  dockerBuildPath: "."

startCommand:
  type: "http"
  configSchema: {}
  exampleConfig: {}

env:
  NODE_ENV: "production"
  MEMORIES_DIR: "/data/memories"